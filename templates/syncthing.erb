description "Syncthing deamon"

start on (starting ufw)
stop on [!023456]

env STNORESTART=yes
env HOME=/opt/syncthing-<%=@version%>/<%=@release%>

respawn

# setuid syncthing
# setgid syncthing

exec /opt/syncthing-<%=@version%>/<%=@release%>/syncthing -home=/opt/syncthing-<%=@version%>/.config/syncthing >> /var/log/syncthing.log 2>&1
