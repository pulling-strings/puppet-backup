#!/bin/sh
#
# PROVIDE: syncthing
# REQUIRE: DAEMON
# KEYWORD: shutdown

. /etc/rc.subr

name=syncthing
rcvar=syncthing_enable

start_cmd="/usr/local/syncthing-<%=@version%>/<%=@release%>/syncthing -home=/usr/local/syncthing/.config/syncthing 1>/var/log/syncthing.stdout 2>/var/log/syncthing.stderr &"
stop_cmd="killall syncthing"

load_rc_config $name

#
# DO NOT CHANGE THESE DEFAULT VALUES HERE
# SET THEM IN THE /etc/rc.conf FILE
#
syncthing_enable=${syncthing_enable-"NO"}
pidfile=${syncthing_pidfile-"/var/run/syncthing.pid"}

run_rc_command "$1"
