start on (filesystem and net-device-up and stopped udevtrigger)

stop on runlevel [06]
respawn

script
  sudo chown <%=@sysuser%> /var/log/copy.log
end script

exec sudo -u <%=@sysuser%> /opt/copy/x86_64/CopyConsole -r=<%=@folder%> -u=<%=@user%> --data=/home/<%=@user%>/.copy  >> /var/log/copy.log

